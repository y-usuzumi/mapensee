$url: null  # will be an argument in the client constructor
~users:
  $url: "${super}/users"
  all:
    $method: "GET"  # default is GET
  get:
    $url: "${super}/<id:number>"
    $method: "GET"
  create:
    $method: "POST"
    $data:
      username: "string"
      password: "string"
  update:
    $url: "${super}/<id:number>/"
    $method: "PUT"
  ~budgets:
    $url: "${super}/budgets"
    all: {}
